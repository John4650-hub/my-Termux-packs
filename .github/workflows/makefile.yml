½name: Makefile CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: configure
      run: |
        echo done
        gh release create ${{ github.run_number }}.0.0
      env:
         GH_TOKEN: ${{ secrets.TK }}
      shell: bash
